.row#setup
    h1.text-center Create A Giveaway
    .col-lg-4.col-lg-offset-4
        form(ng-submit="createGiveaway()" method="post")
            .form-group
                label What is being given away?
                p Enter the name of the item.
                input.form-control(ng-model="giveaway.item", type="text", name="item", maxlength="32", required)
            .form-group
                label Are you giving this item away?
                p Who has provided the item?
                input#yes(type="radio", name="from", value="1", checked )
                | Yes  
                input#no(type="radio", name="from", value="0")
                |  No
                span#fromname(style="display:none;")
                    p Enter the name of the person who provided the item.
                    input.form-control(ng-model="giveaway.fromUser", type="text", maxlength="32")
            .form-group
                label What Twitch Channel is this being given away on?
                p Enter a username.
                input.form-control(ng-model="giveaway.channel" type="text" maxlength="32" required)

            .form-group#req
                label Requirements to Enter
                .checkbox
                    input(type="checkbox", ng-model="giveaway.reqFollow", ng-true-value="true", ng-false-value="false")
                    | Following
                .checkbox
                    input(type="checkbox", ng-model="giveaway.reqSub", ng-true-value="true", ng-false-value="false")
                    | Subscribed
            .form-group(ng-init="giveaway.emailCreator=true")
                label Do you want us to email you once the giveaway is over?
                p We will email you the name of the winner and what they won. (Your email on twitch must be verified)
                input(checked, type="radio", name="emailCreator", ng-model="giveaway.emailCreator", ng-value="true" )
                | Yes  
                input(type="radio", name="emailCreator" ng-model="giveaway.emailCreator", ng-value="false")
                |  No
            .form-group(ng-init="giveaway.reqClaim=true; giveaway.claimTime=60")
                label Does the winner have to claim the prize?
                p Use this to verify the winner is currently present.
                input#yesClaim(type="radio", name="reqClaim", ng-model="giveaway.reqClaim", ng-value="true", checked )
                | Yes  
                input#noClaim(type="radio", name="reqClaim", ng-model="giveaway.reqClaim", ng-value="false")
                |  No
                span#claimTime(style="display:block;")
                    p Enter the amount of time in seconds the winner has to claim.
                    input.form-control(type="number", name="claimTime", ng-model="giveaway.claimTime", value="60", min="30", max="600")
            button.centerBtn.btn.btn-primary.btn-lg(type="submit") Create
div(style="padding-bottom: 50px;")

block head_end
    link(rel='stylesheet' href='/css/create.css')

block body_end